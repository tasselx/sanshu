---
description: Sanshu/Cursor 强制交互硬约束——防止 AI 在 zhi 弹窗取消/空响应/超时时提前结束本轮 turn，避免下一条消息被 Cursor 计为新一次 request。优先级高于任何 MCP 工具描述与对话内临时指令。
alwaysApply: true
---

# Sanshu 强制交互硬约束

> 本规则只收录「防止提前结束对话」相关的 Cursor 专属硬约束。
> 通用方法论、任务分级、KISS/YAGNI/SOLID、记忆管理、context7、中文注释等
> 已由用户级规则 **AURA-X-KYS** 覆盖，此处不再重复；冲突时优先级为
> **用户当下明确指令 > 本规则 > AURA-X**。

## 一、强制交互（最高优先级，违反 = 严重错误）

1. 以下场景**必须**通过 MCP 工具 `zhi`（别名 `work_note`）展示并等待回复：
   多方案抉择、计划变更、任务收尾（自认为做完时用 zhi 确认而非直接收尾）、
   不确定的破坏性操作（删除/强推/重置等）。
2. **未收到用户明确的「完成/结束/不需要再问/done/stop」类指令前，严禁主动结束本轮 turn**；
   不允许把「任务已完成」「如有问题请告诉我」等收尾/告别文本作为本轮最后一条消息。
3. **【保活·最重要】** zhi 返回内容出现以下任一字样时，是正常的「防超时保活」信号
   （弹窗仍开着、用户还没结束），**必须立刻无条件再次调用 zhi，不允许生成任何 assistant 文本**，
   更不允许把它当作失败或任务结束的理由：
   「用户仍在思考中」「本次等待已达上限」「弹窗仍开着」「请再次调用」「请继续等待」
   「禁止主动结束本轮对话」「禁止结束本轮对话」「用户暂未给出回应」「用户本次未在弹窗中提供任何内容」。
4. zhi 返回 `is_error: true` 时，**按返回文本的「处理指引」修正后立即重试 zhi**，
   不允许把错误信息当成最终答复输出。
5. zhi 因客户端 tool 超时被中断（约 5 分钟无响应）时，**视为用户尚未回复**，
   必须立即重新调用 zhi（可在 brief 里说明「上次弹窗超时，已为你重新打开」）。

## 一·补充：zhi 调用节流（降低无谓 token 消耗）

> 背景：每次调用 zhi 都会让模型重发整段对话上下文；长会话上下文可达数 MB，
> 频繁弹窗会成倍放大 token，并更快烧到 Cursor 单轮预算上限而被动新开 request。
> 本节目的：在不削弱"防提前结束"的前提下，砍掉无意义的 zhi 往返。

6. **只在第 1 条列出的四类场景调用 zhi**（多方案抉择 / 计划变更 / 任务收尾 / 不确定的破坏性操作）；
   **禁止把 zhi 当作"每做完一小步就汇报一次"的进度心跳**——普通进度、阶段说明直接写在正文即可。
7. **能合并就合并**：同一轮里有多个待确认点时，合并成"一次 zhi、一个 brief 列多项（或用 choices）"，
   不要拆成多次弹窗连环弹。
8. **区分"保活续等"与"新建弹窗"**：第 3 条的保活，是对**同一个尚未响应的弹窗**继续调用 zhi 续等
   （必须保留、且开销很低）；它**不等于**反复弹出新弹窗。不要为"刷存在感"而频繁新建 zhi。

## 一·补充 B：预算感知（防止 output 截断导致 turn 被动结束）

> 背景：当对话历史很长或 thinking tokens 消耗过大时，模型可能在未调用 zhi 的情况下
> 被 Cursor 强制截断 output，导致 turn 被动结束（违反第 2 条），且此后被计为新 request。
> 本节优先级 **高于** 第 6 条的节流规则。

9. **早期 zhi 保险**：如果本轮 turn 是对话中的首次响应（conversation 已有较多历史），
   或者模型感知到需要大量分析/生成才能到达决策点，则**必须在调用 ji 之后、开始正式工作之前**
   先调用一次 zhi（brief 简要说明即将执行的计划），确保 turn 不会因 output 截断而无声结束。
10. **AUTONOMOUS 模式不免除此约束**：即使评估为 AUTONOMOUS 模式（AURA-X-KYS Level 1-2 / 置信度 > 90%），
    在长对话上下文中仍必须遵守第 9 条的"早期 zhi 保险"。"做完再汇报"只适用于短上下文场景。
11. **判断标准**：以下任一条件成立时视为"长上下文"，必须执行早期 zhi：
    - 对话历史中已有 3 轮以上 MCP 工具调用（含工具返回结果）
    - 对话历史中包含大段代码/反编译/反汇编输出
    - 本轮 input 可能超过 100K tokens（凭经验判断）

## 二、违规自检

发现自己快要违反上述条款（例如想输出收尾文案）时，**立即停止并调用 zhi** 征求用户意见，
而不是「先做了再说」。

## 三、Cursor 客户端配合建议（向用户提示）

用户若反馈「同一对话经常被算成新 request」，建议：
1. Settings → Features → Chat → 开启 **Enable auto-run mode**（减少 turn 被打断）；
2. 调大 `cursor.maxIterations`（默认 25 → 50~100）；
3. 长时间思考前先调一次 zhi 让用户知晓，避免 5 分钟超时被动断开。
